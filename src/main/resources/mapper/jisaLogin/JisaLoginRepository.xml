<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeiglobal.repository.jisaLogin.JisaLoginRepository">
	<select id="findJisaLogins" resultType="hashMap">
		SELECT userId, CONCAT(IFNULL(userFstName,''),IFNULL(userLstName,'')) AS userName,
			CASE jisaCD 
				WHEN '03' THEN '북경'  
				WHEN '06' THEN '호주'
				WHEN '07' THEN '뉴질랜드'
				WHEN '08' THEN '홍콩'	
			END AS jisaName
		FROM globalbiz.Users
		<where>
			userType = 'JA' AND dutyCD = 'J0'
		</where> 
	</select>
</mapper>