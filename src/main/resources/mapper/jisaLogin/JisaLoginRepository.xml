<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeiglobal.repository.jisaLogin.JisaLoginRepository">
	<select id="findJisaLogins" resultType="hashMap">
		SELECT userId, CONCAT(IFNULL(userFstName,''),IFNULL(userLstName,'')) AS userName, deptName
		FROM globalbiz.Users
		<where>
			userType = 'JA' AND deptCD = '00000' AND dutyCD = 'J0'
		</where> 
	</select>
</mapper>